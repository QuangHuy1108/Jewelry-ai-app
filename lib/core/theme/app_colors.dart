import 'package:flutter/material.dart';

class AppColors {
  // Màu chủ đạo chuẩn Luxury
  static const Color primaryGold = Color(0xFFD4AF37);
  static const Color backgroundCream = Color(0xFFF9F6F0);
  static const Color luxuryBlack = Color(0xFF1A1A1A);

  // Màu phụ trợ
  static const Color textGrey = Color(0xFF757575);
  static const Color errorRed = Color(0xFFD32F2F);

  // Màu cho hiệu ứng Loading (Shimmer)
  static final Color shimmerBase = Colors.grey[300]!;
  static final Color shimmerHighlight = Colors.grey[100]!;
}